<Application x:Class="ProductManager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ProductManager"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="Button" x:Key="MyDefaultButton">
            <Setter Property="Margin" Value="6" />
        </Style>
        <Style TargetType="TextBlock" x:Key="PageTitle">
            <Setter Property="FontSize" Value="24" />
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Verdana" />
            <Setter Property="Foreground" Value="Red" />
            <Setter Property="Background" Value="Blue" />
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource MyDefaultButton}" x:Key="MyConfirmButton">
            <Setter Property="Background" Value="Blue" />
        </Style>
    </Application.Resources>
</Application>
